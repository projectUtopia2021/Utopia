import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles({
    card: {
        width: "100%",
        height: "150px",
        boxShadow: "none",
    },
    avatar: {
        width: 100,
        height: 100,
    }
});

export {useStyles};