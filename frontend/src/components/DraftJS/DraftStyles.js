import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles({
    textbox: {
        width: "80%",
        height: "500px",
        boxShadow: "none",
        backgroundColor: "White",
    },
});

export {useStyles};